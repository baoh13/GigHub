@model IEnumerable<GigHub.Models.Gig>
@{
    ViewBag.Title = "title";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Upcoming gigs</h2>

<ul class="gigs">
    @foreach (var gig in Model)
    {
        <li>
            <div class="date">
                <div class="month">@gig.DateTime.ToString("MMM")</div>
                <div class="day">@gig.DateTime.ToString("d ")</div>
                <div class="details">
                    <span class="artist">@gig.User.Name</span>
                    <span class="venue">@gig.Venue</span>
                </div>
            </div>
        </li>
    }
</ul>


@*<ul class="gigs">
    @foreach (var gig in Model)
    {
        <li class="gig">
            <div class="date">
                <div class="month">@gig.DateTime.ToString("MMM")</div>
                <div class="day">@gig.DateTime.ToString("d ")</div>
            </div>
            <div class="details">
                <span class="artist">@gig.User.Name</span>
                <span class="venue">@gig.Genre.Name</span>
            </div>
        </li>
    }
</ul>*@